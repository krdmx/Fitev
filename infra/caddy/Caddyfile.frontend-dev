{
  auto_https off
  admin off
}

http://localhost, http://{$APP_HOST} {
  reverse_proxy host.docker.internal:3000
}

http://{$API_HOST} {
  reverse_proxy api:3001
}

http://{$N8N_HOST} {
  reverse_proxy n8n:5678
}
